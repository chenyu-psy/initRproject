% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_packages.R
\name{install_github_with_v_retry}
\alias{install_github_with_v_retry}
\title{Install a GitHub Package with Automatic v-Prefix Retry}
\usage{
install_github_with_v_retry(repo, ref = NULL, quietly = FALSE, ...)
}
\arguments{
\item{repo}{Character string in the format "account/repo".}

\item{ref}{Character string specifying the tag, branch, or commit to install. If \code{NULL}, the default branch is used.}

\item{quietly}{Logical; if \code{TRUE}, suppresses installation messages.}

\item{...}{Additional arguments passed to \code{remotes::install_github()}.}
}
\value{
Logical. \code{TRUE} if installation succeeded, \code{FALSE} otherwise.
}
\description{
Attempts to install a GitHub package using \code{remotes::install_github()}. If the specified
\code{ref} (tag/branch/commit) looks like a version number (e.g., "0.3.0") and installation fails,
the function automatically retries with a "v" prefix (e.g., "v0.3.0"), which is a common GitHub tag convention.
}
\details{
This function is useful when users specify a version tag without the "v" prefix, but the GitHub repository uses "v" (e.g., "v1.2.3").
It first tries the user-supplied \code{ref}, and if that fails and the ref is a version-like string without "v", it retries with "v" prepended.
}
\examples{
\dontrun{
# Try to install tag "0.3.0" or "v0.3.0" from GitHub
install_github_with_v_retry("chenyu-psy/smartr", ref = "0.3.0")
}
}
